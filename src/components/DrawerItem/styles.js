import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  actionContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 10,
  },
  title: {
    marginStart: 10,
  },
});

// Library imports
import {StyleSheet} from 'react-native';

/**
 * StyleSheet for profile modal
 */
export default StyleSheet.create({
  modalBackground: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    margin: 0,
    display: 'flex',
    overflow: 'visible',
    zIndex: 0,
  },
});

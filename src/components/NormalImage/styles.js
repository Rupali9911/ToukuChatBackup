import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    loaderConatiner: {
        position: 'absolute',
        top: 0,
        bottom: 0,
        left: 0,
        right: 0,
        justifyContent: 'center',
        alignItems: 'center'
    }
});